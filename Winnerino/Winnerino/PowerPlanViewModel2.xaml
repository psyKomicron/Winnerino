<UserControl
    x:Class="Winnerino.PowerPlanViewModel2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Winnerino"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Button Style="{StaticResource HomeButtonStyle}" Click="button_Click">
        <Grid x:Name="contentGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <FontIcon x:Name="powerModeStatusIcon" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1" Grid.Row="0"/>
            <TextBlock Text="{x:Bind PowerPlanName}" Style="{ThemeResource TitleTextBlockStyle}" TextWrapping="Wrap" Grid.ColumnSpan="2" Grid.Row="1"/>
            <TextBlock Text="{x:Bind PowerPlanGuid}" Grid.ColumnSpan="2" Grid.Row="2"/>
        </Grid>
    </Button>
</UserControl>
