<UserControl
    x:Class="Winnerino.AppExpanderHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Winnerino"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="0,10,0,10" Margin="0,0,-25,0" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Viewbox Height="20" Width="20" VerticalAlignment="Center" Grid.RowSpan="2" Grid.Column="0">
            <FontIcon Glyph="{x:Bind Glyph, Mode=OneWay}"/>
        </Viewbox>

        <TextBlock Text="{x:Bind Title, Mode=OneWay}" Margin="15,0,0,0" Grid.Column="1" Grid.Row="0"/>

        <TextBlock Text="{x:Bind Subtitle, Mode=OneWay}" Margin="15,0,0,0" Grid.Column="1" Grid.Row="1" FontSize="12" Opacity="0.7"/>

        <ContentPresenter Content="{x:Bind RightContent, Mode=OneWay}" Grid.Column="2" Grid.RowSpan="2"/>
    </Grid>
</UserControl>
