<UserControl
    x:Class="Winnerino.FileEntryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Winnerino"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" FontWeight="SemiLight" x:DefaultBindMode="OneWay">

    <Grid ColumnSpacing="10" Padding="3,0" Background="{x:Bind Background}">
        <Grid.ColumnDefinitions>
            <!-- icon -->
            <ColumnDefinition Width="22"/>
            <!-- filename -->
            <ColumnDefinition Width="1*"/>
            <!-- file type -->
            <ColumnDefinition Width="2*"/>
            <!-- attributes -->
            <ColumnDefinition Width="Auto"/>
            <!-- size? -->
            <ColumnDefinition MinWidth="20" MaxWidth="66"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <FontIcon Grid.Row="0" Glyph="{x:Bind FileIcon}" HorizontalAlignment="Center" Style="{StaticResource FileAttributeFontIconStyle}" Foreground="{ThemeResource SystemAccentColor}"/>
        <ProgressRing x:Name="FileSizeProgressRing" Grid.Row="0" HorizontalAlignment="Center" IsIndeterminate="True" Width="22" Height="22"/>

        <TextBlock Text="{x:Bind FileName}" VerticalAlignment="Center" Grid.Column="1"/>

        <TextBlock x:Name="FileTypeName" Text="File type" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="2"/>

        <StackPanel
            x:Name="attributesStackPanel" 
            Padding="0" 
            Margin="10,0,0,0" 
            Spacing="{StaticResource StackPanelButtonSpacing}" 
            Orientation="Horizontal"
            VerticalAlignment="Center"
            Grid.Column="3">
            <StackPanel.Resources>
                <Style TargetType="FontIcon" BasedOn="{StaticResource FileAttributeFontIconStyle}"/>
            </StackPanel.Resources>
        </StackPanel>

        <Grid Grid.Column="4" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="FileSizeTextBlock" HorizontalAlignment="Right" Grid.Column="0"/>
            <TextBlock x:Name="FileSizeExtensionTextBlock" HorizontalAlignment="Left" Grid.Column="1"/>
        </Grid>

        <Grid.ContextFlyout>
            <MenuFlyout>
                <MenuFlyoutSubItem Text="File">
                    <MenuFlyoutItem x:Name="openWithFlyoutItem" Text="Open">
                        <MenuFlyoutItem.Icon>
                            <SymbolIcon Symbol="OpenWith"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutSeparator/>
                    <MenuFlyoutItem Text="Delete">
                        <MenuFlyoutItem.Icon>
                            <SymbolIcon Symbol="Delete"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem Text="Rename">
                        <MenuFlyoutItem.Icon>
                            <SymbolIcon Symbol="Rename"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem Text="Copy">
                        <MenuFlyoutItem.Icon>
                            <SymbolIcon Symbol="Copy"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem Text="Cut">
                        <MenuFlyoutItem.Icon>
                            <SymbolIcon Symbol="Cut"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                </MenuFlyoutSubItem>

                <MenuFlyoutItem Text="Copy as path" Click="MenuFlyoutItem_Click">
                    <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="&#xE816;"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutItem x:Name="FileSizeFlyoutItem" Text="Properties" Click="FileSizeFlyoutItem_Click"/>
            </MenuFlyout>
        </Grid.ContextFlyout>
    </Grid>
</UserControl>
