<UserControl
    x:Class="Winnerino.FileEntryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Winnerino"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" x:DefaultBindMode="OneWay">

    <Grid ColumnSpacing="10" Padding="0,0,15,0" Background="{x:Bind Background}">
        <Grid.ColumnDefinitions>
            <!-- icon -->
            <ColumnDefinition Width="22"/>
            <!-- filename -->
            <ColumnDefinition Width="3*" MaxWidth="900"/>
            <!-- file type -->
            <ColumnDefinition Width="1*"/>
            <!-- attributes -->
            <ColumnDefinition MaxWidth="100"/>
            <!-- size? -->
            <ColumnDefinition MinWidth="20" MaxWidth="66"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <FontIcon 
            Grid.Row="0" 
            Glyph="{x:Bind FileIcon}" 
            HorizontalAlignment="Center" 
            Style="{StaticResource FileAttributeFontIconStyle}" 
            Foreground="{ThemeResource SystemAccentColor}"/>
        <ProgressRing x:Name="FileSizeProgressRing" Grid.Row="0" HorizontalAlignment="Center" IsIndeterminate="True" Width="22" Height="22"/>

        <TextBlock x:Name="FileNameTextBlock" Text="{x:Bind FileName}" HorizontalAlignment="Stretch" TextTrimming="CharacterEllipsis" Grid.Column="1"/>

        <TextBlock x:Name="FileTypeName" Text="File type" VerticalAlignment="Center" Grid.Column="2"/>

        <StackPanel
            x:Name="attributesStackPanel" 
            Padding="0" 
            Margin="10,0,0,0" 
            Spacing="{StaticResource StackPanelButtonSpacing}" 
            Orientation="Horizontal"
            VerticalAlignment="Center"
            Grid.Column="3">
            <StackPanel.Resources>
                <Style TargetType="FontIcon" BasedOn="{StaticResource FileAttributeFontIconStyle}"/>
            </StackPanel.Resources>
        </StackPanel>

        <Grid Grid.Column="4" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="FileSizeTextBlock" HorizontalAlignment="Right" Grid.Column="0"/>
            <TextBlock x:Name="FileSizeExtensionTextBlock" HorizontalAlignment="Left" Grid.Column="1"/>
        </Grid>

        <ContentDialog 
            x:Name="RenameContentDialog"
            DefaultButton="Primary" 
            PrimaryButtonText="Rename" 
            SecondaryButtonText="Cancel">
            <Grid RowSpacing="20" Padding="10">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <TextBox x:Name="RenameTextBox" PlaceholderText="New name" Grid.Row="0"/>

                <CheckBox x:Name="GenerateUniqueNameCheckBox" Content="Generate unique name" Grid.Row="1"/>
            </Grid>
        </ContentDialog>
    </Grid>
    
    <UserControl.ContextFlyout>
        <MenuFlyout>
            <MenuFlyoutSubItem Text="File">
                <MenuFlyoutItem x:Name="OpenWithFlyoutItem" Text="Open" Click="OpenWithFlyoutItem_Click">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="OpenWith"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem x:Name="DeleteFlyoutItem" Text="Delete" Click="DeleteFlyoutItem_Click">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="Delete"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutItem x:Name="RenameFlyoutItem" Text="Rename" Click="RenameFlyoutItem_Click">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="Rename"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutItem x:Name="CopyFlyoutItem" Text="Copy" Click="CopyFlyoutItem_Click">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="Copy"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutItem x:Name="CutFlyoutItem" Text="Cut" Click="CutFlyoutItem_Click">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="Cut"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
            </MenuFlyoutSubItem>

            <MenuFlyoutSeparator/>
            <MenuFlyoutItem Text="Copy as path" Click="MenuFlyoutItem_Click">
                <MenuFlyoutItem.Icon>
                    <FontIcon Glyph="&#xe71b;"/>
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
            <MenuFlyoutItem x:Name="OpenInExplorerFlyoutItem" Text="Open containing folder" Click="OpenInExplorerFlyoutItem_Click">
                <MenuFlyoutItem.Icon>
                    <FontIcon Glyph="&#xec50;"/>
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
            <MenuFlyoutItem x:Name="FileSizeFlyoutItem" Text="Properties" Click="FileSizeFlyoutItem_Click">
                <MenuFlyoutItem.Icon>
                    <FontIcon Glyph="&#xea40;"/>
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
        </MenuFlyout>
    </UserControl.ContextFlyout>

    <ToolTipService.ToolTip>
        <ToolTip x:Name="UserControlToolTip" Width="{x:Bind Width}" Opened="ToolTip_Opened"/>
    </ToolTipService.ToolTip>
</UserControl>
